package com.delta.rdfdrools

import com.hp.hpl.jena.rdf.model.Resource;

global java.util.List list

rule Init when
    res : Resource(LocalName == "Injection_Molding_Machine")
  then
    System.out.println(res.getURI());
  end
