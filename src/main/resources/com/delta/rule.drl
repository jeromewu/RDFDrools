package com.delta

import com.delta.Context;

rule Tag1 
  when
    $ctx : Context(getLiteralFloat("accontrol:hasTag1Temp") > 22)
  then
    System.out.println("Tag1 Temp: " + $ctx.getLiteralFloat("accontrol:hasTag1Temp"));
  end
